{
  "name": "Recompta",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tesseract-ocr tesseract-ocr-fra tesseract-ocr-eng && pip install -r backend/requirements.txt && echo 'Setup terminé. Lancez: cd backend && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload'",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Recompta",
      "onAutoForward": "openBrowser"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python"]
    }
  }
}
