# Déposez ici vos factures fournisseurs réelles (PDF ou images scannées)

Formats acceptés : `.pdf`, `.png`, `.jpg`, `.jpeg`, `.webp`, `.tiff`

Puis lancez :

```bash
cd backend
python scripts/test_upload_folder.py
```

Ou importez-les via l'interface web http://localhost:8000
